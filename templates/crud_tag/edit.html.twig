{% extends 'base.html.twig' %}

{% block title %}Modifier Tag {{ tag.tagName }}{% endblock %}

{% block body %}
    <!-- Page Header-->
    <header class="masthead">
        <div class="container position-relative px-4 px-lg-5">
            <div class="row gx-4 gx-lg-5 justify-content-center">
                <div class="col-md-10 col-lg-8 col-xl-7">
                    <div class="site-heading">
                        <h1>Modifier Tag {{ tag.tagName }}</h1>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <!-- Main Content-->
    <div class="container-fluid px-4 px-lg-5">
        <div class="row gx-4 gx-lg-5 justify-content-center">
            <div class="col-md-10 col-lg-8 col-xl-7">
                <h1>Modifier Tag</h1>
                {{ include('crud_tag/_form.html.twig', {'button_label': 'Modifier'}) }}
                <a href="{{ path('app_crud_tag_index') }}">Retour à la liste</a>
                {{ include('crud_tag/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
